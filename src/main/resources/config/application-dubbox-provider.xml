<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
    					http://www.springframework.org/schema/beans/spring-beans.xsd 
    					http://www.springframework.org/schema/context 
						http://www.springframework.org/schema/context/spring-context.xsd 
    					http://code.alibabatech.com/schema/dubbo 
    					http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 加载配置文件 -->
	<bean id="dubbox_propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">	
		<property name="ignoreUnresolvablePlaceholders" value="true" />
		<property name="locations">
			<value>classpath:config/dubbox.properties</value>
		</property>
	</bean>

	<!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="mcd-microservice-provider" />
    
	<!-- 使用zookeeper注册中心暴露服务地址 -->
	<!-- 
	<dubbo:registry address="zookeeper://10.20.153.10:2181" />
	<dubbo:registry protocol="zookeeper" address="10.20.153.10:2181" />
	<dubbo:registry address="zookeeper://10.20.153.10:2181?backup=10.20.153.11:2181,10.20.153.12:2181" />
	<dubbo:registry protocol="zookeeper" address="10.20.153.10:2181,10.20.153.11:2181,10.20.153.12:2181" /> -->
    <dubbo:registry address="${dubbox.registry.address}" />
    
	<!-- 使用web服务器发布应用 ，contextpath、port必须与web 容器的配置一样 -->
	<dubbo:protocol name="rest" port="${dubbox.protocal.rest.port}" contextpath="${dubbox.protocal.rest.contextpath}" server="servlet" />
	
	<!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="${dubbox.protocal.dubbo.port}" payload="${dubbox.protocol.payload}" 
    	threadpool="cached" threads="1000" iothreads="20" queues="0" buffer="10240" accepts="10000" />
</beans>